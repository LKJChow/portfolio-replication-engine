# Lower bounds, not exact pins. Exact pins written against one interpreter
# break on a newer one: Python 3.14 has no wheels for several 2024 releases,
# so pip either falls back to building from source or fails outright.

# --- needed for SEC ingestion (the only hard dependency to get started) ---
httpx>=0.28
python-dotenv>=1.0

# --- analysis ---
pandas>=2.3
numpy>=2.3
statsmodels>=0.14.4

# --- service ---
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7

# --- persistence (not required until the database layer lands) ---
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.10
alembic>=1.14

# --- dev ---
pytest>=8.3
